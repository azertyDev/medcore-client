import { OrganizationsListView } from 'src/sections/organization/views';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Список организаций',
};

const Registry = () => <OrganizationsListView />;

export default Registry;
